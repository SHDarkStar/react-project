// در این فایل App  فراخوانده شده تا رندر روی ان صورت بگیرد
import ReactDOM from 'react-dom/client';
import App from './App.tsx';

ReactDOM.createRoot(document.getElementById('root')!).render(<App />);
